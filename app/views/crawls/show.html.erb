
<div class="d-flex justify-content-center">
  <div class="card mx-5 mt-2" style="overflow:hidden; border-radius: 35px; width:1250px">
    <%= render "shared/map" %>
  </div>
</div>

<div class="d-flex justify-content-center">
  <div class="row mt-2 d-flex justify-content-start">
    <% @bars.sample(3).each do |bar| %>
      <%= render "shared/barcards", bar: bar %>
    <% end %>
  </div>
</div>
 <div class="d-flex justify-content-center my-2">
  <button type="button" class="btn btn-primary">Save this crawl</button>
</div>
